x = int(input('輸入整數X：')) #輸入同時轉換資料類型，否則默認為string。
y = int(input('輸入整數Y：')) #input()讀入資料，括號內可輸出提示。
print('X+Y=', x + y)

#除法
#單斜：小數結果
#雙斜：四捨五入取整
#int(a/b)：向下取整

#次方：底數**指數

#字串：單引號 / str
#整數：int
#小數： float
